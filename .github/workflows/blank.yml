name: "ENV variable Demo"

on:
  workflow_dispatch:

jobs:
  custom_demo:
    runs-on: ubuntu-latest

    steps:
      - name: Set the value
        run: echo "this is a variable value" >> $GITHUB_ENV

      - name: Display the variable
        run: echo ${{ env.ENV_VAR }}
